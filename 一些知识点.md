页面传数据
1、url
2、全局data
3、缓存

nginx
start nginx : 启动nginx服务

nginx -s reload  ：修改配置后重新加载生效

nginx -s reopen  ：重新打开日志文件

tasklist /fi "imagename eq nginx.exe"

nginx -s stop	fast shutdown
nginx -s quit	graceful shutdown

DOM，前端性能优化方法，浏览器的渲染原理

react-router-dom  
页面跳转 用 Link 不用 a ，可以减少不必要的html加载

with (try catch)中的catch  eval 会改变作用域链

不要用fill()来给数组填充对象或数组，因为当一个对象被传递给 fill方法的时候, 填充数组的是这个对象的引用。也就是说数组中每一个值都会引用同一个对象。

正则表达式

html：5    vscode中快速创建html模板

伪类可以连写，表示选择元素同时在几种状态下应用样式，
伪元素不能连写。

浏览器渲染  回流 重绘  BFC

解析阻塞 vs. 异步js
我们可以在script标签中加入async属性来告诉浏览器，在执行js脚本时同时执行DOM构造。

事件 发布订阅






